@page "/db"
@using WagerMate.Data
@using WagerMate.Services
@using WagerMate.Services.impl
<h3>Database</h3>

<p>This page displays data from Postgresdb</p>

@inject IUserService service;

<p>@service.CreateUser(init())</p>
@code {

    public User init()
    {
        User user = new User();
        user.Name = "tobi";
        user.Id = 234;
        user.Email = "tobi@gmail.com";
        user.Password = "1234";
        return user;
    }
}